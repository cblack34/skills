[project]
name = "cblack34-skills"
version = "0.1.0"
description = "Cross-harness marketplace tooling for Clayton's personal agent skills."
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = []

[tool.uv]
package = false
