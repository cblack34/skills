# Address PR Review

Address unresolved review comments on the current branch’s PR from any reviewer, validating each finding independently before fixing or pushing back.

The shared skill instructions are in `skills/address-pr-review/SKILL.md`.

## Development

Run the marketplace validator from the repository root after changes:

```bash
uv run --locked scripts/validate.py
```
